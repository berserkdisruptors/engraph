# Engraph Context Index — Routing Table
# Regenerated deterministically by `engraph graph`.
# Source of truth: individual convention/verification YAML files.
# If this file drifts or is missing, regenerate with `engraph graph`.
version: "1.0"
generated_at: 2026-04-08T18:17:32.428Z
codegraph_hash: 2a56ccf91206
conventions:
  - id: agent-template-conventions
    path: conventions/agent-template-conventions.yaml
    applies_to_modules:
      - templates/agents
    provenance: manual
  - id: error-handling-in-templates
    path: conventions/error-handling-in-templates.yaml
    applies_to_modules:
      - templates/*
    provenance: manual
  - id: migration-conventions
    path: conventions/migration-conventions.yaml
    applies_to_modules:
      - commands/upgrade/migrations
    provenance: manual
  - id: naming-conventions
    path: conventions/naming-conventions.yaml
    applies_to_modules:
      - "*"
    provenance: manual
  - id: skill-creation-conventions
    path: conventions/skill-creation-conventions.yaml
    applies_to_modules:
      - templates/skills/*
    provenance: manual
  - id: skill-naming-conventions
    path: conventions/skill-naming-conventions.yaml
    applies_to_modules:
      - templates/skills/*
    provenance: manual
  - id: testing-conventions
    path: conventions/testing-conventions.yaml
    applies_to_modules:
      - "*"
    provenance: manual
  - id: yaml-file-extension-convention
    path: conventions/yaml-file-extension-convention.yaml
    applies_to_modules:
      - "*"
    provenance: manual
verification:
  - id: release-package-verification
    path: verification/release-package-verification.yaml
    triggered_by_modules:
      - commands/init
      - commands/upgrade
      - templates/*
    provenance: manual
  - id: testing-standards
    path: verification/testing-standards.yaml
    triggered_by_modules:
      - "*"
    provenance: manual
